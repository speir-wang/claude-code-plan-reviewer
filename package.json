{
  "name": "claude-code-plan-reviewer",
  "version": "1.0.0",
  "description": "Interactive browser-based plan review for Claude Code via MCP",
  "type": "module",
  "main": "dist/mcp-server.js",
  "bin": {
    "plan-reviewer-mcp": "./dist/mcp-server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/bundle-browser.js",
    "start": "node dist/mcp-server.js",
    "dev": "tsx src/mcp-server.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run test/unit",
    "test:integration": "vitest run test/integration",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "reviewer": "tsx test/e2e/reviewer.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "diff": "^8.0.4",
    "express": "^5.2.1",
    "marked": "^18.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "@types/supertest": "^6.0.0",
    "@vitest/coverage-v8": "^4.1.4",
    "esbuild": "^0.28.0",
    "jsdom": "^29.0.2",
    "playwright": "^1.59.1",
    "supertest": "^7.2.2",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.4"
  },
  "engines": {
    "node": ">=20"
  }
}
